set wildignore+=*/docs,*/deps

setlocal formatoptions-=t " Don't wrap code
setlocal formatoptions+=o " Autoinsert comment char on 'o' or 'O'
setlocal formatoptions+=r " Autoinsert comment char on <ret>
setlocal formatoptions+=q " Allow formatting comments with gq
