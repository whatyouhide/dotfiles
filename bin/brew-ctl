#!/bin/sh

set -e

main() {
    local _cmd="$1"
    shift
    for formula in "$@"; do
        echo "$_cmd" $formula
        launchctl "$_cmd" "$(brew --prefix $formula)/homebrew.mxcl.$formula.plist"
    done
}

main "$@"
