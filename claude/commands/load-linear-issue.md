---
description: Load a Linear issue into context using the `linear` CLI.
allowed-tools: Bash(linear issue view:*)
argument-hint: [issue-id]
---

Load Linear issue $1 into context using the Linear CLI with:

```
linear issue view <issue-id>
```

Use `linear issue --help` if anything goes wrong.

