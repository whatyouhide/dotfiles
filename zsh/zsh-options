# Set name of the theme to load.
# Optionally, if you set this to "random", it'll load a random theme each
# time that oh-my-zsh is loaded.
ZSH_THEME=$DOTFILES_ZSH_THEME

# oh-my-zsh plugins.
plugins=(z brew osx zsh-syntax-highlighting)

# Mmmm. Cd into this stuff everytime.
# http://robots.thoughtbot.com/cding-to-frequently-used-directories-in-zsh
# (the `setop auto_cd` line is inside zshrc if you were wondering).
cdpath=($HOME $HOME/Sites $HOME/Code $HOME/Dropbox)
