# For stuff not related to the environment (which is taken care of in
# ~/.extra-env.zsh and ~/.env-setup.zsh).

# For example:
#
#     cdpath=($HOME/Code $HOME/tmp)
#
# or:
#
#     eval "$(direnv hook zsh)"
